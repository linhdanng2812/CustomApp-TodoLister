<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
        <item name="actionOverflowButtonStyle">@style/MenuIconColor</item>
        <item name="android:textColor">@color/newtask_text_color</item>
        <item name="android:windowBackground">@color/background_color_light</item>
        <item name="android:drawableTint">@color/colorAccent</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="MenuColor" parent="Theme.MaterialComponents">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="MenuIconColor" parent="Widget.AppCompat.Light.ActionButton.Overflow">
        <item name="tint">#ffffff</item>
    </style>

    <style name="ChangeTextColor" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
